# ==================== 服务配置 ====================
# 服务监听地址
HOST=127.0.0.1

# 服务监听端口
PORT=8002

# 调试模式（开发环境设为 True，生产环境设为 False）
DEBUG=False

# ==================== 路径配置 ====================
# 图片存储目录（必须配置，绝对路径）
# 示例: /Users/your-username/images 或 /var/www/file_parse/images
IMAGE_DIR=/path/to/your/image/directory

# ==================== 日志配置 ====================
# 日志级别: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ==================== 文件解析配置 ====================
# 默认文本块大小（字符数）
DEFAULT_CHUNK_SIZE=1000

# 默认文本块重叠大小（字符数）
DEFAULT_CHUNK_OVERLAP=100

# ==================== 图片配置 ====================
# 最大图片尺寸（宽度, 高度）
# 注意: 这是一个元组，格式必须是 (width, height)
MAX_IMAGE_SIZE=[1024, 1024]