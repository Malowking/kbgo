# Docker Compose 环境变量配置示例
# 复制此文件为 .env 并根据需要修改

# ============================================
# 完整模式（docker-compose --profile full up -d）
# ============================================
# 使用容器内的 PostgreSQL
# DB_HOST=postgres
# DB_PORT=5432
# DB_USER=kbgo
# DB_PASS=kbgo123
# DB_NAME=kbgo
# DB_TYPE=pgsql

# POSTGRES_HOST=postgres
# POSTGRES_PORT=5432
# POSTGRES_USER=kbgo
# POSTGRES_PASS=kbgo123
# POSTGRES_DB=kbgo

# ============================================
# 默认模式（docker-compose up -d）
# ============================================
# 连接宿主机的 PostgreSQL（默认值）
DB_HOST=host.docker.internal
DB_PORT=5432
DB_USER=your_local_user
DB_PASS=your_local_password
DB_NAME=kbgo
DB_TYPE=pgsql

POSTGRES_HOST=host.docker.internal
POSTGRES_PORT=5432
POSTGRES_USER=your_local_user
POSTGRES_PASS=your_local_password
POSTGRES_DB=kbgo

# 向量存储类型
VECTORSTORE_TYPE=pgvector

# ============================================
# 通用配置
# ============================================
# 如果不配置环境变量，默认使用 host.docker.internal 连接本地服务